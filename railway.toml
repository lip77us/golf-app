[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate --noinput && python manage.py createsuperuser --noinput || true && gunicorn my_golf_app.wsgi --bind 0.0.0.0:$PORT --log-file -"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
